CREATE TABLE IF NOT EXISTS customer_features (
    user_id BIGINT PRIMARY KEY,
    avg_flight_discount_log FLOAT,
    flight_discount_rate_log FLOAT,    
    discount_per_km_log FLOAT,
    avg_hotel_discount_log FLOAT,
    hotel_discount_rate_log FLOAT,
    days_last_trip_log FLOAT,
    sessions_per_month_log FLOAT,
    avg_session_duration_seconds_log FLOAT,
    page_click_per_session_log FLOAT,
    avg_flight_travel_days_log FLOAT,
    avg_seats_log FLOAT,
    avg_checked_bags_log FLOAT,
    avg_distance_km_log FLOAT,
    flight_booking_value_log FLOAT,
    avg_hotel_nights_log FLOAT,
    hotel_booking_value_log FLOAT,
    cancellation_rate_log FLOAT,
    avg_flight_discount_scaled FLOAT,
    flight_discount_rate_scaled FLOAT,    
    avg_hotel_discount_scaled FLOAT,
    hotel_discount_rate_scaled FLOAT,
    age_scaled FLOAT,
    is_married_scaled FLOAT,
    has_children_scaled FLOAT,
    days_last_trip_scaled FLOAT,
    sessions_per_month_scaled FLOAT,
    avg_session_duration_seconds_scaled FLOAT,
    page_click_per_session_scaled FLOAT,
    avg_flight_travel_days_scaled FLOAT,
    avg_seats_scaled FLOAT,
    avg_checked_bags_scaled FLOAT,
    avg_distance_km_scaled FLOAT,
    flight_booked_scaled FLOAT,
    flight_booking_value_scaled FLOAT,
    avg_rooms_scaled FLOAT,
    avg_hotel_nights_scaled FLOAT,
    hotel_booked_scaled FLOAT,
    hotel_booking_value_scaled FLOAT,
    cancellation_rate_scaled FLOAT,
    flight_discount_score FLOAT,
    hotel_discount_score FLOAT,
    user_score FLOAT,
    engagement_score FLOAT,
    flight_travel_score FLOAT,
    hotel_travel_score FLOAT,
    cancellation_score FLOAT
);
